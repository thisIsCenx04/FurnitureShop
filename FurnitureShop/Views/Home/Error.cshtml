@model FurnitureShop.ViewModels.ErrorViewModel

<h1 class="text-danger">Error</h1>
<p class="text-muted">Có lỗi xảy ra.</p>

@if (Model.ShowRequestId)
{
    <p><strong>RequestId:</strong> <code>@Model.RequestId</code></p>
}
